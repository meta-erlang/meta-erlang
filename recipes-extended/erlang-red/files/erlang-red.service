[Unit]
Description=Breadboard Programming for Erlang inspired by Node-RED 
Wants=network-online.target
After=network-online.target remote-fs.target

[Service]
Type=exec
User=erlang-red
Group=erlang-red
WorkingDirectory=/var/lib/erlang-red
ExecStart=/usr/lib/erlang-red/bin/erlang_red foreground

Restart=on-failure
RestartSec=5

# Writes erlang-red configuration into WorkingDirectory
Environment="RELX_OUT_FILE_PATH=/var/lib/erlang-red"

[Install]
WantedBy=multi-user.target
